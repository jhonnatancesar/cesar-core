# Exemplo histórico anterior à release 1.0.0; NÃO é o deploy oficial.
# Usar ../compose.yaml e ../docs/deployment/docker.md para a stack suportada.
# Mantido apenas como referência da topologia externa anterior.

services:
  cesar-core:
    build: ..
    ports:
      - "127.0.0.1:8100:8100"
    networks:
      - cesar-platform
    env_file:
      - ../.env

networks:
  cesar-platform:
    external: true
